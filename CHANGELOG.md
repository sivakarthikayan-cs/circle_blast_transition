## 0.1.0

* Initial release.
* `CircleBlastRoute` — drop-in `PageRouteBuilder` with four built-in blast styles.
* `CircleBlastTransition` — standalone widget for custom route integration.
* `BlastStyle` enum: `radialCenter`, `random`, `topToBottom`, `diagonal`.
* Zero external dependencies; pure Flutter / Dart.
